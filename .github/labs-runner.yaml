apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: pewanalytics-runner
  namespace: github-runners

spec:
  replicas: 1
  template:
    spec:
      repository: pewresearch/pewanalytics_dev
      image: 458280294434.dkr.ecr.us-east-1.amazonaws.com/labs-actions-runner@sha256:d7f0def8e9dbb7acb66336b9973c19a024490437bed8449ffa1727f6c0dca49f
      imagePullPolicy: Always
      serviceAccountName: labs-runner
      labels:
        - labs-runner
      dockerEnabled: false
      dockerdContainerResources:
        limits:
          cpu: "4.0"
          memory: "8Gi"

        requests:
          cpu: "1.0"
          memory: "2Gi"

      env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: labs-runner
              key: AWS_ACCESS_KEY_ID

        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: labs-runner
              key: AWS_SECRET_ACCESS_KEY

    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: "true"
